# 本地后端环境配置模板。复制到仓库根目录 .env.backend.local 后填写真实值；该文件已被 Git 忽略。
# 生产/CI 应通过宿主环境或密钥管理器注入同名变量，不使用此文件。

# 本地 MySQL identity 库密码
DB_PASSWORD=

# 各 JWT 密钥至少 32 字节，且两者必须不同。
STORE_JWT_SECRET=
ADMIN_JWT_SECRET=

# Base64 编码的稳定 32 字节 AES-256 密钥。若存在 gateway API Key 密文，必须使用原密钥。
DREAMY_GATEWAY_AES_KEY=
